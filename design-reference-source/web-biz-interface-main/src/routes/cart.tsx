import { createFileRoute,Link } from "@tanstack/react-router";import { useState } from "react";import { StoreShell } from "@/components/store-shell";import { products,formatMoney } from "@/components/store-data";import { Button } from "@/components/button";
export const Route=createFileRoute("/cart")({head:()=>({meta:[{title:"Giỏ hàng — BẠO.sneak"},{name:"description",content:"Kiểm tra sản phẩm, size, số lượng và tổng tiền trước khi đặt hàng COD."},{property:"og:title",content:"Giỏ hàng — BẠO.sneak"},{property:"og:description",content:"Giỏ hàng mua giày tại BẠO.sneak."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Page});function Page(){const [items,setItems]=useState([{...products[0],size:40,qty:1},{...products[1],size:42,qty:1}]);const total=items.reduce((s,x)=>s+x.price*x.qty,0);return <StoreShell><main className="page-wrap"><div className="page-heading"><p className="eyebrow">Đơn hàng của bạn</p><h1>Giỏ hàng</h1><p>Sản phẩm chưa được giữ tồn kho cho đến khi đặt hàng thành công.</p></div><div className="grid gap-6 lg:grid-cols-[1fr_360px]"><div className="space-y-3">{items.map((x,i)=><div key={x.slug} className="flex gap-4 rounded-lg border border-line bg-surface-glass p-4"><img src={x.image} alt={x.name} className="size-24 rounded-lg object-cover"/><div className="flex-1"><h2 className="font-display font-bold">{x.name}</h2><p className="text-sm text-muted-foreground">Size {x.size} · Còn hàng</p><p className="mt-2 font-bold">{formatMoney(x.price)}</p></div><div className="flex flex-col items-end justify-between"><button onClick={()=>setItems(items.filter((_,idx)=>idx!==i))} className="text-xs text-coral">Xóa</button><div className="flex rounded-lg border border-line"><button onClick={()=>setItems(items.map((it,idx)=>idx===i?{...it,qty:Math.max(1,it.qty-1)}:it))} className="size-9">−</button><span className="grid size-9 place-items-center">{x.qty}</span><button onClick={()=>setItems(items.map((it,idx)=>idx===i?{...it,qty:it.qty+1}:it))} className="size-9">+</button></div></div></div>)}</div><aside className="h-fit rounded-lg border border-line bg-surface p-5"><h2 className="font-display text-xl font-bold">Tóm tắt</h2><div className="mt-5 space-y-3 text-sm"><div className="flex justify-between"><span>Tiền hàng</span><span>{formatMoney(total)}</span></div><div className="flex justify-between"><span>Phí giao hàng</span><span>30.000₫</span></div><div className="flex justify-between border-t border-line pt-4 font-display text-xl font-bold"><span>Tổng cộng</span><span>{formatMoney(total+30000)}</span></div></div><Link to="/checkout"><Button className="mt-5 w-full">Tiến hành đặt hàng</Button></Link><p className="mt-3 text-center text-xs text-muted-foreground">Thanh toán COD khi nhận hàng</p></aside></div></main></StoreShell>}
